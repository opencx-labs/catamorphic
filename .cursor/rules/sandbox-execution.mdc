---
description: Workflow execution architecture — sandbox providers, run lifecycle, and instrumentation constraints
globs: ["packages/sandbox/**", "packages/runtime/**", "packages/core/src/services/**", "packages/fastify-plugin/src/routes/playground.ts"]
alwaysApply: false
---

# Sandbox & Execution Architecture

## Sandbox Provider Interface

Execution is abstracted behind `SandboxProvider` / `SandboxManager` so the underlying provider can be swapped. Providers live in **vendor plugin packages** (see `docs/decisions/0008`): `CloudflareSandboxProvider` in `@catamorphic/cloudflare` (the default — see `docs/decisions/0004` and `CLOUDFLARE.md`) and `DaytonaSandboxProvider` in `@catamorphic/daytona`. `@catamorphic/sandbox` holds only the vendor-neutral contracts and must not depend on vendor SDKs. Hosts construct their chosen provider explicitly at boot and pass it to `createCatamorphic({ sandboxProvider })` — there is no env-sniffing selection helper. Never call the Cloudflare or Daytona SDKs directly from routes — go through the service layer.

Providers handed to `CatamorphicCore` are automatically wrapped with `instrumentSandboxProvider` (OpenTelemetry spans for create/exec/upload/etc.) — do not double-wrap, and keep the wrapper's passthrough of the optional `hydrateWorkspace` method intact when extending the provider contract.

## Execution Model

1. Production runs resolve deployed `origin/main` and retain that SHA. Test runs use the caller's dev files plus overlays and deliberately retain no SHA.
2. **Pre-generate the run ID** before entering the sandbox. The run row is inserted into `workflow_runs` with status `running` before the sandbox call.
3. Pass project files + run ID + trigger data into the sandbox.
4. The sandbox runs the canonical `@catamorphic/runtime` harness with environment values supplied through provider execution options, never shell assignments.
5. On completion (or error), transactionally update the run row and replace its step rows.

## Two-Sandbox Model

- **Production execution sandbox**: fresh immutable snapshot tied to the deployed git commit. Used to run production workflows reliably.
- **Dev (coding agent) sandbox**: mutable, per-(project, external user). Tracked in `project_sandboxes` (migration `008`). The coding agent has read/write access to the project checkout inside the sandbox; after each turn `AgentSessionsService` syncs changed files back into the user's dev working copy as an uncommitted draft. Sessions record `base_commit_sha` so every AI conversation is traceable to a code version.
- **Test run directory**: disposable run-specific copy inside the caller's dev sandbox. It is deleted after execution and never receives a source revision.

## Step Instrumentation Constraints

**Do NOT** use:
- Regex-based source-level code transformation to inject instrumentation.
- Global variable / module-level monkey-patching of ES module exports (ES modules are immutable after evaluation — monkey-patching silently fails).

`@catamorphic/parser` produces an execution transform that wraps exact step call expressions with the node IDs from the parsed graph. Transform only the disposable execution copy, never user source or step declarations.

## Provider Availability

The server degrades gracefully when **no** provider is configured (neither the `CLOUDFLARE_SANDBOX_API_URL` + `CLOUDFLARE_SANDBOX_API_KEY` pair nor `DAYTONA_API_KEY`): both production and test run endpoints return `503 Service Unavailable`. The client surfaces this as a user-visible error, not a crash. Integration tests that require a specific provider use the `describeIf` skip pattern.

## Coding Agent

The agent is pluggable behind `CodingAgentProvider` (`docs/decisions/0009`). Implementations are vendor plugin packages: `@catamorphic/flue` (**flagship** — the Flue harness runs in the host server process and drives the dev sandbox remotely via the `catamorphicSandbox` adapter; model keys never enter the sandbox) and `@catamorphic/codex` (Codex SDK). Hosts pass one via `createCatamorphic({ codingAgent })`, which enables `core.agentSessions` and the agent HTTP routes. Sessions persist to `agent_sessions` / `agent_messages` with the git commit SHA at session start; per-project agent skills live in the repo under `.agents/skills/` (`docs/decisions/0010`).
