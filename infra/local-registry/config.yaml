# Local npm registry for Catamorphic development (see infra/local-registry/README.md).
# Serves @catamorphic/* from local publishes; everything else proxies npmjs.
storage: ./storage
# All interfaces: build sandboxes (microVMs) reach the host by its LAN/gateway address.
listen: 0.0.0.0:4873
uplinks:
  npmjs:
    url: https://registry.npmjs.org/
packages:
  '@catamorphic/*':
    access: $all
    publish: $all
  '**':
    access: $all
    proxy: npmjs
log: { type: stdout, format: pretty, level: warn }
