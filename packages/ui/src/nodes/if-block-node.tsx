import type { NodeProps } from "@xyflow/react";

export function IfBlockNode(_props: NodeProps) {
  return (
    <div
      style={{
        width: "100%",
        height: "100%",
        pointerEvents: "none",
      }}
    />
  );
}
